class Solution {
public:
    vector<int> sumZero(int n) {  
      vector<int> an;
     if (n % 2 == 1){
         an.push_back(0);
     }  
    for (int a = 1; a <= n / 2; a++){
            an.push_back(a);
            an.push_back(-a); 
     }
       return an;
    }
};
